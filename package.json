{
  "name": "untanglit",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "nx build untanglit",
    "dev": "nx run untanglit:dev",
    "dev:construction": "nx run construction:dev",
    "start": "nx run untanglit:start",
    "lint": "nx run untanglit:lint",
    "postinstall": "cd untanglit && npm install && cd ../modern_templates/construction && npm install"
  },
  "devDependencies": {
    "nx": "22.5.3"
  }
}
